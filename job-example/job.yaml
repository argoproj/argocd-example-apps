apiVersion: batch/v1
kind: Job
metadata:
  name: dms-migrations
spec:
  template:
    spec:
      containers:
      - name: dms-migrations
        image: curlimages/curl
        command:
          - "curl"
          - "https://172.31.112.1:5001/swagger/index.html"
      restartPolicy: Never
  backoffLimit: 4
