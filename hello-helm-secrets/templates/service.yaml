apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-helm-secrets.fullname" . }}
  labels:
    {{- include "hello-helm-secrets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-helm-secrets.selectorLabels" . | nindent 4 }}
