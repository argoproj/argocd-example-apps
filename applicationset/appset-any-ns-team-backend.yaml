apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: services
  namespace: team-backend
spec:
  generators:
    - list:
        elements:
          - service: api
          - service: worker
  template:
    metadata:
      name: '{{service}}'
      # Generated Applications will be in team-backend namespace (same as ApplicationSet)
    spec:
      project: team-backend
      source:
        repoURL: https://github.com/argoproj/argocd-example-apps
        targetRevision: HEAD
        path: lightweight/app-b
      destination:
        server: https://kubernetes.default.svc
        namespace: 'team-backend-{{service}}'
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true
