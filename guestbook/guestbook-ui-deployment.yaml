apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-ui
  namespace: guestbook
  annotations:
   codefresh.io/product: guestbook-test
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-ui
  template:
    metadata:
      labels:
        app: guestbook-ui
    spec:
      containers:
      - image: amankinenko/ks-guestbook-demo:0.5
        name: guestbook-ui
        ports:
        - containerPort: 80
