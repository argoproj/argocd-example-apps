apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: guestbook-ui
  namespace: guestbook
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - backend:
          service:
            name: guestbook-ui 
            port:
              number: 80
        path: /
        pathType: Prefix
    host: guestbook.com